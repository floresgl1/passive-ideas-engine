# Daily Ideas — 2026-09-03

Sourced from `profile.md` (`last_refresh: 2026-08-30`, unchanged for the fourth consecutive digest
since 08-31's refresh). The last three digests already pulled the schematic-layout engine, the golf
export-corruption bug, the Stripe rejection regression pattern, a first capacitor/transient reach
into signal integrity, CoCircuit's undo/redo, the netlist suffix parser, poker-llm's 0-byte-file
commit-claim gap, and a Ruiz/GMIN solver-mode combination — all left untouched today. Today instead
reaches for four pieces of material that have never anchored a digest before, three of them sitting
in the profile since well before this refresh but never mined: poker-llm's *other*, distinct honest-
scope gap — two unvendored shadcn/ui components that mean the React dashboard "will not build from a
clean checkout without an extra manual step," never used until now (the 0-byte evaluation files were
mined on 08-24/09-02, but this build-gap sibling detail was not); golf's `--left` CLI flag plus the
`_fold` boundary resolution at exactly ±90° that shipped alongside it, isolated from the confidence-
gate and dedup work already mined around it; `ledger-core`'s Stripe-specific input adapter, published
via the same secretless PyPI pipeline as `ledger-tieout` but never itself the subject of an idea (only
the currency-formatting bug and the zero-dependency packaging story have been mined from that project);
and, for the first time, the profile's memory-hierarchy/caches want — sitting untouched in Direction &
Intent since 08-28's one passing mention — reached via the schematic/diagram-rendering skill instead
of restated as a bare goal. Sub-agent template packs, UART/SPI/I2C, timers/PWM, EDF/RM scheduling, the
Iron Law/ALU line, and the OAuth chokepoint/manifest material stay untouched again, all mined
repeatedly already. Today's four land as a full [Experience]/[Leverage]/[Passive]/[Stretch] spread,
none weakened to force it. No idea crosses the keystone — the visual/JS circuit-simulator direction
stays closed, and nothing below re-touches it.

### 1. Clean-Checkout Build Guard — [Experience]
- competence: unlabeled
- labeled_at: —

**Idea:** Build a small CI check that clones a repo fresh into an empty directory, runs only the documented public install/build commands with no local caches or manually-added files, and fails loudly if the build doesn't succeed — using poker-llm's own real gap as the reference case: its React dashboard imports two shadcn/ui components the README itself admits are "not vendored yet," so the frontend silently does not build from a clean checkout despite the repo's own "production ready" claim.
**Leverages:** Epistemic discipline about one's own system [strong] (this is the same "measurement vs. claim" instinct that already caught poker-llm's 0-byte evaluation files, applied to a sibling gap) plus Testing discipline [strong] and CI/CD and scheduled automation [strong] — GitHub Actions, fail-closed patterns already proven in finance_bot and stripe-reconciler.
**One new thing to learn:** simulating a genuinely clean checkout inside CI (a scratch container or workdir with no npm/pip cache carried over from a prior job) rather than trusting a runner's warm cache to mask the exact gap that bit poker-llm.
**Why it's worth it:** "builds locally, doesn't build clean" is a specific, real, already-proven failure mode in this person's own repo, invisible to any test suite that never starts from zero — a small, drop-in Actions template that catches it is a template/tool other solo developers can install repeatedly, not a one-off audit.
**First step today:** write a one-job GitHub Actions workflow that `git clone --depth 1`s poker-llm into a fresh runner, runs its documented `npm install && npm run build` with no cache, and confirm it fails on the current `main` for the exact shadcn/ui reason already known.

### 2. Mirror-Symmetry Test Kit — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract the pattern behind golf's left-handed support — a `--left` flag threaded through five CLI scripts plus the `_fold` boundary ambiguity at exactly ±90° resolved by a fixed rule (folds to +90 regardless of point order) rather than merely pinned by one test — into a small, reusable test-design checklist and harness for any orientation-dependent measurement pipeline: assert that mirroring the input mirrors the output exactly, and that boundary angles have one defined, tested resolution instead of an implicit one.
**Leverages:** Computer vision on real footage [strong] and Testing discipline [strong] — specifically the golf project's own discipline of pinning an ambiguous boundary case with a rule, not just a passing test.
**One new thing to learn:** generalizing "left-handed golfer" into a caller-supplied mirror axis so the harness works for any pipeline with a left/right or clockwise/counterclockwise ambiguity (gait analysis, other racket/club sports, robotics arm symmetry), not just golf swings.
**Why it's worth it:** orientation bugs are a well-known, recurring failure class in motion-tracking and CV pipelines, and this person just shipped a concrete, tested fix for exactly one instance of it; packaging the checklist plus a small pytest-based harness turns an already-solved problem into a sellable starter kit for other CV developers building single-sided or symmetry-sensitive measurement tools.
**First step today:** write the harness's first generic assertion — given a pipeline function and a caller-supplied "mirror" transform for its input, assert `pipeline(mirror(x)) == mirror(pipeline(x))` — and run it against golf's own `--left` flag as the first proof case.

### 3. Multi-Processor Adapter Pack for ledger-tieout — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** `ledger-core` is already published to PyPI as `ledger-tieout` with a proven Stripe input adapter, zero runtime dependencies, and a secretless OIDC release pipeline; build one or two more processor adapters (PayPal, Square, or Plaid transaction exports) on the same architecture and sell them as a small paid add-on package layered on the free core library, rather than adding features to the free core itself.
**Leverages:** Relational / double-entry data modeling [strong] — the adapter boundary (unclassifiable transactions route to Suspense with a named reason, Stripe's payout-vs-balance-transaction double-booking already caught and refused) is already processor-agnostic by design; this reuses that architecture rather than rebuilding it.
**One new thing to learn:** reading one new processor's real export format closely enough to find its own double-booking or ambiguous-transaction traps, the way the Stripe payout/balance-transaction collision was found — a research task, not a new architecture.
**Why it's worth it:** the free core already proves the value proposition (680 tests, live on PyPI, a documented release pipeline with no long-lived token) — a bookkeeper or small-business owner using a second payment processor is the exact next customer, and a second adapter is a bounded, mechanical extension of code that already works rather than a new product.
**First step today:** pull a real (anonymized or sandbox) PayPal or Square transaction export and sketch its field mapping into `ledger_core`'s existing transaction shape, flagging any transaction type that has no obvious existing category — the same first move that surfaced Stripe's payout double-booking risk.

### 4. Memory-Hierarchy Trace Visualizer — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Build a small direct-mapped (then set-associative) cache simulator over a synthetic memory-access trace, and render its hit/miss behavior per level using the same breadth-first layout-and-SVG-rendering approach just proven in circuit-simulator's schematic renderer — the first concrete step into "memory hierarchy/caches," a hardware-track want named in Direction & Intent with zero shipped inventory anywhere in the profile.
**Leverages:** Schematic/diagram layout and rendering [emerging] (circuit-simulator's `web/layout.py` + `web/render_svg.py` — dependency-free SVG generation, deterministic layout rules) plus Python service and pipeline architecture [strong]; the new-domain content (cache behavior) draws on the quarantined hardware direction, so this stays a Stretch rather than a Leverage/Passive presented as ready.
**One new thing to learn:** the cache-simulation logic itself — decomposing an address into tag/index/offset and tracking hits/misses across levels — while the rendering and layout machinery is entirely reused, not reinvented.
**Why it's worth it:** memory hierarchy is explicitly named as a hardware-track want with no shipped evidence anywhere in the profile, and this is the smallest step that actually produces something (a working simulator plus a legible diagram) rather than restating the want; a small teaching visualizer aimed at students learning cache behavior is a real, if modest, sellable content asset, and the highest-risk part — turning structured data into a readable diagram — is already built and tested.
**First step today:** write a minimal direct-mapped cache simulator against a synthetic access trace (a fixed sequence of memory addresses), logging hit/miss counts per address with no rendering yet, to confirm the simulation logic is correct before reusing any layout code.
