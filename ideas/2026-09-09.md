# Daily Ideas — 2026-09-09

Sourced from `profile.md` (`last_refresh: 2026-09-06`, unchanged for the third consecutive digest
off that stamp, and substantively unchanged since 09-04 — six days of digests now running against
the same real content, confirmed by diffing today against the 09-07/09-08 copies rather than
assumed). Checked against the full `ideas/` history (37 prior files) before picking today's
material, not just the recent run. Confirmed already exhausted and skipped again: the whole
CoCircuit/WebMCP vein (starter kit, embeddable widget, cross-validation harness, power-budget
checker, undo/redo, noise-floor guard, the WebMCP×poker-llm pairing), circuit-simulator's
schematic-layout engine, netlist suffix parser, Ruiz/GMIN combo, capacitor/RC and inductor/RLC
extensions, and the op-amp/gyrator golden-circuit pack; golf's confidence gate, `--left` flag,
iMessage export bug, falsification harness, backswing-implausibility check, circular-angle math,
the three quieter robustness fixes, and yesterday's CI icon/launch-screen kit; stripe-reconciler's
whole security checklist (mined five-plus times), the OAuth chokepoint, manifest fixes, and the
`STRIPE_CLIENT_ID` drift; poker-llm's 0-byte-file gap, unbuildable-dashboard gap, tournament layer,
and strategic-prompt feature API; studyprof's extraction endpoint and dormant quiz helper (used
four times running) and its two combinations from yesterday; finance_bot's Tavily search and SHAP
explainability; and every hardware `[conceptual]` line (Iron Law/ALU eight-plus times, EDF/RM,
memory hierarchy, polling-vs-interrupt, serial protocols, timers/PWM) plus sub-agent template packs
and CrewAI/ReAct, both mined five-plus times. Today instead reaches for three pieces of material
confirmed, by direct search, to have never anchored a digest before: golf's `ThemeExtension`-based
Flutter theming system and its clip retention/deletion policy — both shipped this window, sitting
in the Flutter/Dart `[emerging]` skill description since the profile's own most recent refresh, and
never once used as idea material; the Dart fault-test rewrite's "mutation-verified" claim, distinct
from the boundary tests themselves (already the reference case for 09-06's fuzzer pitch) — the
verification *method* itself has never been the anchor; and, for the first time, a direct pairing
between ledger-core's zero-sum property-test pattern and poker-llm's own chip/bankroll accounting —
two proven capabilities named together nowhere in the history, and a different invariant target
from 08-16's already-shipped generic money-field test generator (that tool scans arbitrary code for
money-shaped fields; this applies the same conservation principle to one specific, already-built
game-state machine). No fourth idea ships today — a Stretch pick would mean re-angling hardware
`[conceptual]` material mined at least once every week since the axis's keystone closed on 08-28/29,
and Direction & Intent is otherwise fully covered by existing Passive/Alpaca/ESCALATE ideas already
in the history; three genuinely fresh, well-differentiated ideas beat a forced fourth. Today lands
as [Passive]/[Experience]/[Leverage] — no Stretch; a strong Stretch tomorrow needs either new
flagship-repo commits or the next weekly refresh, not a ninth reach into the same four exhausted
hardware lines. No idea crosses the keystone — that direction closed for good on 08-28/29 and
nothing below touches it.

### 1. Flutter Local-Media Starter Kit — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract golf's shipped `ThemeExtension`-based theming system and its clip
retention/deletion policy into a standalone, published Flutter/Dart starter package that any
local-video-recording mobile app can drop in, sold once on pub.dev or as a paid starter template to
other solo Flutter developers.
**Leverages:** Flutter/Dart mobile [emerging] — this week's real, shipped `ThemeExtension` theming
system and clip retention/deletion logic in golf, both genuine Flutter work never yet extracted as
their own product.
**One new thing to learn:** packaging an app-specific theming layer and a local-storage retention
policy as an installable, app-agnostic Dart package and publishing it to pub.dev — a distribution
mechanism already proven once for Python (`ledger-tieout` on PyPI) but never attempted for Dart.
**Why it's worth it:** local-media retention (when to auto-delete old recordings, how much on-device
storage to keep) is a small, real, commonly-skipped design problem for any camera-capture app; golf
already built and shipped both a consistent theming system and a working retention policy this
window, so this is extraction, not new engineering.
**First step today:** pull the `ThemeExtension` class definitions and the clip retention/deletion
functions out of golf's codebase into their own Dart files with all golf-specific data stripped
out, and confirm they compile as a standalone package.

### 2. Dart Mutation-Verified Test Harness — [Experience]
- competence: unlabeled
- labeled_at: —

**Idea:** Generalize this week's Dart fault-test rewrite — four-probe boundary tests confirmed
mutation-verified — into a small, reusable mutation-testing harness for Dart/Flutter test suites:
deliberately introduce small code mutations (flip a comparator, off-by-one a boundary) and confirm
the existing suite actually fails, filling a real gap since Dart has no mature `mutmut`/`cosmic-ray`
equivalent the way Python does.
**Leverages:** Testing discipline [strong] — the "a threshold test that can't detect a threshold
change is vacuous" principle already proven across Python projects, applied here to Dart's own
suite for the first time.
**One new thing to learn:** writing the mutation-injection mechanism itself, most likely via Dart's
own analyzer/AST tooling, since there is no off-the-shelf Dart mutation-testing library to call into
the way Python's `mutmut` would let this step be skipped.
**Why it's worth it:** this exact discipline already caught a real gap by hand this week in golf's
own Dart fault tests; turning the one-off manual mutation check into a reusable harness produces a
tool sellable to any Flutter developer whose suite has never been checked for this specific
vacuousness, in an ecosystem that currently has nothing like it.
**First step today:** take one of golf's four Dart boundary-probe tests, hand-introduce the smallest
possible mutation to the code it targets (flip a comparison operator), and confirm the existing test
fails — the harness's first manually-verified case, before any automation is built around it.

### 3. Tournament Chip-Conservation Checker — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Apply ledger-core's proven zero-sum property-test pattern directly to poker-llm's own
tournament engine — not the generic "scan any codebase for money-shaped fields" tool already built
from this pattern, but the specific conservation invariant itself: a property test verifying that
total chips across all players plus the pot equal the tournament's starting chip count after every
dealt hand, elimination, and blind escalation — packaged as a small testing library for any
turn-based game or wagering engine that moves a conserved resource between players.
**Leverages:** Relational/double-entry data modeling [strong] (ledger-core's already-proven
zero-sum property test) and Game-engine implementation [emerging] (poker-llm's own state machine) —
two proven capabilities combined for the first time, targeting a genuinely different kind of
invariant than the earlier money-field-scanning tool this same pattern already produced.
**One new thing to learn:** adapting a zero-sum invariant from ledger-core's flat list-of-transactions
shape to a turn-based state machine's sequence of mutating game-state snapshots, where the "sum"
must hold at every intermediate step, not only at a final reconciliation.
**Why it's worth it:** poker-llm's own honest-scope notes already flag two places where a commit
claimed more than it shipped (stubbed evaluation files, an unbuildable dashboard); a
conservation-invariant test catches a genuinely different bug class — a state-machine logic error
that silently creates or destroys chips — and the underlying test pattern is already built and
proven, so this is adaptation, not new invention.
**First step today:** write one property test that runs poker-llm's own hand-history data through a
chip-sum check after every action in a single saved hand, and confirm it passes on a real hand
poker-llm already logged.

<!-- prompt-version: 2026-08-14.1 -->
