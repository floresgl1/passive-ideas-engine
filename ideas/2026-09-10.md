# Daily Ideas — 2026-09-10

### 1. Agent-Operability Audit Tool — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** A CLI/GitHub Action that audits a web API — from its OpenAPI spec or live traffic — against a scored "agent-operability" checklist (idempotency support, structured/enumerated error taxonomies, dry-run/preview endpoints, rate-limit signaling, machine-actionable error text) and returns a report flagging exactly what would make the API unsafe or ambiguous for an LLM agent to drive.
**Leverages:** Agent-accessible application interfaces (WebMCP) [emerging] (CoCircuit's actionable-error-for-a-tool-calling-model discipline), Applied web application security [strong] (stripe-reconciler's reasoned-not-cargo-culted security checklist habit), REST API design [strong], LLM agent engineering [strong] (finance_bot's enumerated error categories and bounded-step discipline).
**One new thing to learn:** defining a general, checklist-style "agent-operability" rubric that scores an arbitrary third party's API, rather than the one bespoke tool surface built by hand for CoCircuit's own single application.
**Why it's worth it:** every team wiring MCP/function-calling onto their product right now is guessing at what makes an API "agent-safe" without a name for the checklist; this person has already built one product from scratch specifically to be agent-driven (CoCircuit) and a second with disciplined, enumerated error handling for an agent loop (finance_bot) — turning that lived experience into a scored audit tool is a new, sellable product, not a re-skin of either repo.

### 2. Leakage Auditor for Backtests — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** A standalone Python library/CLI that ingests a quant or time-series ML pipeline's own train/test split boundaries, feature timestamps, and label windows, and flags concrete temporal-leakage patterns — non-chronological splits, features computed with future data, labels that assume a static regime — sold once per project or metered as a CI check for solo algo-trading and time-series ML developers who don't have this discipline built in yet.
**Leverages:** Supervised ML on time series with leakage awareness [strong] (finance_bot's walk-forward retraining, chronological splits, regime-aware dynamic labeling) and Applied statistical reasoning under uncertainty [strong] (finance_bot's CI-lower-bound-vs-breakeven and sample-size intuition).
**One new thing to learn:** generalizing finance_bot's own hand-built leakage safeguards into a rule set that inspects an arbitrary caller's pipeline metadata and names the specific leakage pattern it finds, rather than just preventing it internally on one codebase's own data structures.
**Why it's worth it:** leakage is the single most common, hardest-to-self-detect bug in retail/solo time-series ML — most builders never carefully separate leading from lagging indicators or validate chronological splits the way finance_bot's own pipeline was forced to after real failure modes; packaging that hard-won discipline as an external auditor is a small, bounded, sellable tool built entirely from muscle already proven under real stakes.

`last_refresh` still reads 2026-09-06, now the fourth consecutive digest on unchanged material — today continues in invention mode with two new ventures, distinct from 09-09's silent-failure-monitoring SaaS and reference-firmware kit.

<!-- prompt-version: 2026-09-09.2 -->
