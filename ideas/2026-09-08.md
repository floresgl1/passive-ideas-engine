# Daily Ideas — 2026-09-08

Sourced from `profile.md` (`last_refresh: 2026-09-06`, unchanged since yesterday's digest, which was
itself the first to read this refresh — the 09-06 weekly-refresh commit landed "no substantive
profile change," and the real content has now been stable since the 09-04 refresh that added
studyprof: three refresh timestamps, one actual set of facts, going on five days). Checked against
the full `ideas/` history (36 prior files, ~140 ideas) rather than just the recent run before
picking today's material. Confirmed already mined and skipped again today: golf's iMessage
export-corruption bug, the Podfile host-gating fix (both from the same 08-21 fix, re-verified by
direct grep), every circuit-simulator/CoCircuit schematic-visualizer and solver-internals angle
(four separate visualizer framings, WebMCP three times, undo/redo, noise floors, viewport-follow,
op-amp/gyrator validation — the last two used just yesterday), the full stripe-reconciler security
checklist, the OAuth-chokepoint and env-var-drift material, every [conceptual] hardware line
(scheduling, Iron Law, polling-vs-interrupt, memory hierarchy, pipelining, serial protocols,
timers/PWM) each mined at least once already, and poker-llm's rules engine/tournament layer/build
gap. Direct search also confirms golf's CI icon-and-launch-screen asset stamping (picked up this
week, distinct from the already-mined permission-string guard bug) has never anchored a digest.
Today leans on that fresh thread plus two genuinely new *combinations* of already-shipped pieces
that have never been paired before: studyprof's real PyMuPDF extraction endpoint wired to
visual-search-engine's real embedding/vector-DB stack (the one prior "document search" idea, from
before studyprof existed, only ever proposed a hypothetical CLIP-to-text swap), and studyprof's
dormant Nebius quiz-generator wired through poker-llm's actual multi-LLM tournament harness rather
than run alone. Three ideas ship today, not four — after five days on unchanged material, a fourth
idea would have meant re-angling something already mined ([Experience] specifically has nothing
fresh left: CoCircuit's missing test suite, a second ledger-tieout processor adapter, and golf's
pose-detection dedup are all already used). A strong [Experience] idea tomorrow needs either new
commits on a flagship repo or the next weekly refresh, not a fifth framing of today's material. No
idea crosses the keystone — that direction closed for good on 08-28/29 and nothing below touches it.

### 1. Flutter Release-Asset Compliance Checker — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Generalize this week's golf CI work — the build pipeline now stamps app icon and
launch-screen assets correctly across build configs, on top of the earlier fix for a weak
`grep -q` guard that let 1 of 3 configs ship with a missing camera-permission string — into a
small, reusable pre-flight checker that verifies icon, launch-screen, and permission-string assets
are all correctly generated across every build config before a release, sold as a paid CI step or
CLI to other solo Flutter developers.
**Leverages:** AI-assisted development workflow at the tooling level [strong] and CI/CD and
scheduled automation [strong] — golf's macOS compile-check workflow and this week's icon/launch-
screen stamping already prove the exact CI pattern this reuses.
**One new thing to learn:** enumerating the actual, complete set of iOS/Android release-asset
requirements (icon sizes per platform, launch-screen variants, `Info.plist`/manifest permission
strings) as one automatable checklist, rather than the single permission-string check golf has
built and verified so far.
**Why it's worth it:** missing or mismatched release assets are a common, mechanical, easily
automated App Store/Play Store rejection cause; golf has already built and CI-verified two pieces
of this exact checklist this quarter, so packaging it as a standalone pre-flight tool is a bounded
extraction, not new engineering.
**First step today:** write down golf's actual icon/launch-screen/permission-string CI checks as
one ordered checklist script, and run it against golf's own repo as its first self-test — it should
pass cleanly, since golf's CI already enforces all three.

### 2. PDF Knowledge-Base API — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** Wire studyprof's shipped PDF-upload text-extraction endpoint (`POST /upload/` →
`extract_text()` via PyMuPDF) directly into visual-search-engine's shipped embedding/vector-DB/
FastAPI stack, producing one hosted "upload your PDFs, get semantic search over them" API sold as
a metered subscription to solo consultants and small teams — the first idea to combine these two
actual, working pieces rather than the earlier (pre-studyprof) proposal to hypothetically swap
CLIP for a text encoder.
**Leverages:** Document ingestion / file-upload text extraction [emerging] (studyprof) and
Embeddings and vector retrieval / RAG [emerging] (visual-search-engine) — both real, shipped, and
never yet exercised against each other's input.
**One new thing to learn:** swapping visual-search-engine's CLIP image encoder for a text
embedding model and tuning chunk size/overlap against studyprof's actual extracted PDF text, since
neither piece has been run against the other's data before.
**Why it's worth it:** both halves already exist and work independently — extraction is proven,
retrieval infrastructure is proven — so this needs an adapter, not new infrastructure; a hosted
ingest-and-search API is naturally metered/subscription revenue with little ongoing labor once
built.
**First step today:** pipe one of studyprof's already-extracted PDF text outputs into
visual-search-engine's embedding/ChromaDB pipeline with a text embedding model swapped in for CLIP,
and confirm a semantic query returns the correct chunk.

### 3. Multi-Model Quiz-Quality Benchmark — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Finish wiring studyprof's dormant `generate_question()` Nebius helper into a live route
(the same missing step named in an earlier digest's Upload-to-Quiz idea), then run the same
uploaded material through it and through 1-2 other LLMs using poker-llm's model-agnostic
player-interface and head-to-head orchestration pattern, producing a small benchmarking report on
which model generates the best quiz questions from identical source text — sold to edtech teams
choosing an LLM vendor for content generation.
**Leverages:** LLM agent engineering [strong] (poker-llm's model-agnostic adapters and
head-to-head/round-robin orchestration, applied outside a game for the first time) and Document
ingestion / file-upload text extraction [emerging] (studyprof) — genuinely combining two shipped
pieces that have so far only ever been extended alone.
**One new thing to learn:** defining a scoring rubric for "quiz question quality" (or an
LLM-judged comparison) and adapting poker-llm's win/loss tournament structure — built for a game
with an objective winner — to a subjective content-quality comparison; one clear new thing, not a
new evaluation framework from scratch.
**Why it's worth it:** the two pieces already exist separately and in working order; nobody has
put studyprof's real (if unverified) LLM call through an actual multi-model comparison harness
before, and doing so both ships studyprof's single highest-leverage missing route and produces a
sellable, structured comparison artifact.
**First step today:** wire `generate_question()` into a live FastAPI route, run one uploaded PDF
chunk through it and through one other model via poker-llm's adapter interface, and manually
compare the two outputs side by side.

<!-- prompt-version: 2026-08-14.1 -->
