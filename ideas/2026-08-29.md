# Daily Ideas — 2026-08-29

Sourced from `profile.md` (`last_refresh: 2026-08-23`, unchanged for the fifth consecutive digest —
still no new weekly refresh). The last three digests (08-24, 08-27, 08-28) already mined golf's
confidence gate, `pose_pipeline.py` consolidation, the `--left` mirroring flag, the `2b05186`
guardrail-drop bug, stripe-reconciler's spent OAuth grant/manifest fix, and finance_bot's statistical
toolkit applied outside finance — so today deliberately reaches for four things checked against the
full `ideas/` history that have never anchored a digest before: ledger-core's `REVIEW_ALREADY_BOOKED`
double-booking guard (Stripe reporting one payout as both a balance transaction and a `Payout`
object), which has only ever appeared as a supporting detail inside 08-20's hosted-API pitch, never
as its own idea; golf's UI-pass honesty note that the "drill demo-clip container" is infrastructure
only (`media: ''` for all 12 drills, "coming soon") — dormant content infrastructure never proposed
as an asset before; this week's Python/Dart parity test (byte-identical frame indices on 20 fixture
clips, proving the port while deliberately leaving the window-semantics divergence open) — the
parity-harness pattern itself has never been extracted as a standalone tool, distinct from the
epistemic-discipline-as-content pitches already made; and real-time schedulability analysis
(`[conceptual]`) paired for the first time with finance_bot's circuit-breaker/staleness-detection
production discipline (`[strong]`) rather than analysis alone, which 08-07's schedulability analyzer
and 08-09's poll-vs-interrupt simulator already covered separately. The circuit-simulator visualizer
is again not proposed — seven-plus prior pitches, zero commits since 2026-08-14 (now three weeks
idle) — and neither is the Ruiz-equilibration guard, the epoch-seam pattern, or the manifest auditor,
all already fully mined. Today's four land as [Leverage]/[Passive]/[Experience]/[Stretch] — a full
spread, none weakened to force it. No idea crosses the keystone today; nothing here touches the
visual/JS circuit-simulator axis.

### 1. Double-Booking Guard Library — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract ledger-core's `REVIEW_ALREADY_BOOKED` check — catching Stripe reporting one real
payout as both a balance-transaction line and a separate `Payout` object, and refusing to book the
second copy — into a standalone, processor-agnostic dedup library that flags when two API shapes
describe the same underlying event, for any payments integration (PayPal, Plaid, Shopify payouts)
that risks the same silent double-count.
**Leverages:** Relational / double-entry data modeling [strong], specifically ledger-core's dispatch
logic (`entries_for`) and its explicit Suspense-routing discipline — a real double-booking risk
already caught and tested, not a hypothetical.
**One new thing to learn:** generalizing the Stripe-specific "balance-transaction vs. Payout object"
matching rule into a configurable event-identity function that other processors' adapters can supply.
**Why it's worth it:** the hard part — recognizing that two differently-shaped API responses describe
one cash movement, and refusing to double-book it — is already solved and tested in ledger-core; this
is an extraction of a proven guard, not new financial-modeling work, and every processor integration
with overlapping report shapes hits this exact risk.
**First step today:** pull `REVIEW_ALREADY_BOOKED`'s matching logic out of ledger-core's Stripe
adapter into a standalone function that takes two event lists and a caller-supplied identity-match
rule instead of Stripe's specific field names.

### 2. Drill-Demo Clip Library — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** Produce a licensable set of short instructional demo clips matched one-to-one against
golf's existing 12-drill, fault-to-drill prescription taxonomy — the container for this already
shipped in the UI this week (`media: ''`, "coming soon") but sits empty — and license the clip pack
plus its JSON fault→drill mapping to other golf-coaching apps or independent coaches who need
matched instructional content but have no biomechanics engine of their own to generate the mapping.
**Leverages:** golf's closed diagnose → prescribe → verify loop [strong] and its already-defined
12-drill corrective taxonomy — the hard classification work (which fault maps to which drill) is
done; only the content itself is missing.
**One new thing to learn:** video production and a licensing/distribution model for a content asset,
distinct from any code-shipping skill already proven.
**Why it's worth it:** the taxonomy and the UI slot for it both already exist and are sitting dormant
("coming soon" for all 12 drills); recording once and licensing repeatedly is the definition of a
passive content asset, and no code work is blocking it.
**First step today:** record one demo clip for the single most common fault flag's drill, and wire it
into the existing `media` field to prove the container works end to end before producing the other 11.

### 3. Port-Parity Harness Toolkit — [Experience]
- competence: unlabeled
- labeled_at: —

**Idea:** Generalize this week's Python→Dart parity test — requiring byte-identical frame indices
against 20 fixture clips while deliberately leaving the window-semantics divergence open by design —
into a reusable pytest/CI toolkit that runs two implementations of the same function against a shared
fixture set, reports byte-for-byte mismatches, and lets the caller mark specific divergences as
intentionally-held-open (with a required reason string) rather than silently passing or hard-failing.
**Leverages:** Testing discipline [strong] and the epistemic-discipline through-line's specific
instance of holding a known divergence open on purpose rather than mirroring an unvalidated change
(golf) — building this turns a one-off internal test pattern into a documented, enforceable policy.
**One new thing to learn:** designing a fixture-driven comparison harness generic enough to accept any
two callables and any fixture format, instead of golf's hardcoded Python/Dart pose-frame comparison.
**Why it's worth it:** anyone maintaining two parallel language implementations of the same logic
hits this exact problem — a divergence that's real but not yet worth chasing needs to be visible and
tracked, not buried in a skipped test or silently tolerated; golf already proved the pattern works,
this just makes it reusable.
**First step today:** write the harness's core comparison function generic over two arbitrary
callables and a fixture directory, then re-point golf's existing 20-fixture Python/Dart parity check
at it to prove the extraction preserves the original result.

### 4. Deadline-Miss Circuit Breaker Simulator — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Build a small real-time task-set simulator (EDF and Rate Monotonic scheduling, a
precedence-graph model, hyperperiod computation) that layers on finance_bot's production-reliability
pattern rather than stopping at feasibility math — a halt-flag that trips when deadline misses cross a
threshold within a rolling window, mirroring finance_bot's loss-limit circuit breaker, so the sim
demonstrates graceful degradation under overload instead of only reporting infeasible/feasible.
**Leverages:** Production observability and failure handling [strong] (finance_bot's staleness
detection, halt-flag/circuit-breaker logic, transition-only alerting) applied for the first time to
`[conceptual]` real-time scheduling analysis (EDF, LDF, RM, hyperperiod/schedulability reasoning),
rather than the two staying separate as they have in every prior scheduling or reliability pitch.
**One new thing to learn:** implementing EDF/RM scheduling simulation and hyperperiod computation
correctly over a synthetic task set — one well-defined algorithm family, reusing an already-proven
reliability pattern rather than inventing a new one.
**Why it's worth it:** it reaches the stalled hardware-track direction (idle three weeks now) with a
portfolio piece that visibly combines two different `[strong]`/`[conceptual]` skills instead of
restating either alone, which is a stronger signal for the target hardware/firmware track than a bare
scheduling calculator.
**First step today:** implement hyperperiod computation and an EDF feasibility check over one fixed
synthetic task set, printing feasible/infeasible, as the smallest slice before adding the halt-flag
layer.
