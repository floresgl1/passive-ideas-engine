# Daily Ideas — 2026-09-04

Sourced from `profile.md` (`last_refresh: 2026-08-30`, unchanged for the fifth consecutive digest
since 08-31's refresh). The four prior digests off this refresh already pulled the schematic-layout
engine, golf's iMessage export-corruption bug, the Stripe-rejection regression pattern, a first
capacitor/RC reach into signal integrity, CoCircuit's undo/redo, the netlist suffix parser, poker-llm's
unvendored-dashboard build gap, the Ruiz/GMIN solver-mode combination, and (09-03) golf's `--left`
mirroring flag plus a first memory-hierarchy reach — all left untouched today. Today instead reaches
for four pieces of material checked against the full `ideas/` history and never used as an idea's core
before: golf's falsification-harness mechanism (`score.py`/`labels.json`, the `label_basis` distinction
that keeps a video-verified label from being pooled with a weaker sheet-derived one — the exact
machinery that let this week's work refute `detect_phases` at 0/10 and `locate_swing` at 2/10 instead
of trusting an eyeballed improvement), only ever cited as supporting proof inside 08-28's unrelated
statistical-toolkit pitch and never itself the anchor; golf's reinstated backswing-must-outlast-
downswing implausibility check, distinct from the already-extracted confidence-based per-landmark gate
(08-27) — a different failure class entirely, since a frame can pass every confidence check and still
produce a physically impossible phase ordering; poker-llm's tournament orchestration layer (10-level
blind escalation, elimination tracking, head-to-head/round-robin/knockout modes), sitting in the
profile since 08-18 and never mined at all, even though the hand evaluator and prompt-builder pieces
around it have been (08-21, 08-31); and a second, deeper reach into signal integrity — an inductor and
genuine second-order RLC behavior — past the single-capacitor RC step response already shipped as an
idea on 09-01. Sub-agent template packs, the epoch-seam/OAuth-chokepoint material, op-amp/gyrator test
composition, and the Iron Law/ALU line stay untouched again, all mined repeatedly already. Today's four
land as a full [Leverage]/[Passive]/[Experience]/[Stretch] spread, none weakened to force it. No idea
crosses the keystone — the visual/JS circuit-simulator direction stays closed, and nothing below
re-touches it.

### 1. Physical-Plausibility Guard — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract this week's reinstated backswing-must-outlast-downswing check from golf — a sanity guard that rejects a measurement as implausible when it violates a known physical ordering constraint, distinct from the confidence-based per-frame gate already shipped and extracted (08-27) — into a small, reusable "plausibility assertion" library for motion-measurement pipelines: declare a constraint once (e.g. phase A must outlast phase B) and flag any computed result that violates it, independent of per-frame detection quality.
**Leverages:** Computer vision on real footage [strong] and the epistemic-discipline through-line's habit of catching an internally-impossible result rather than trusting a number that merely computed without error.
**One new thing to learn:** generalizing golf's one hardcoded ordering rule into a small declarative constraint format (e.g. `duration(A) > duration(B)`) a caller can register for their own pipeline's phases, instead of one fixed check.
**Why it's worth it:** this is a genuinely distinct failure class from the confidence gate already extracted — a frame can pass every per-landmark confidence check and still produce a physically impossible phase ordering, exactly what this reinstated rule catches — so it's new material for any biomechanics/gait/sports-motion pipeline, not a repackaging of the already-shipped gate.
**First step today:** pull the reinstated backswing/downswing ordering check out of golf's phase-detection code into a standalone `assert_ordering(durations, constraint)` function, and add one test using golf's own phase data as the reference case.

### 2. Label Provenance Guard — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract golf's `score.py`/`labels.json` `label_basis` mechanism — distinguishing video-derived ground-truth labels from weaker spec/sheet-derived ones so a scoring function can never silently pool them — into a small, standalone Python scoring library any ML/CV project can use to tag ground truth by evidence strength and get a warning or refusal instead of a blended accuracy number when weak and strong labels get mixed.
**Leverages:** Epistemic discipline about one's own system [strong] — the exact mechanism that let this week's golf work falsify `detect_phases` at 0/10 and `locate_swing` at 2/10 rather than trust an eyeballed improvement — plus Testing discipline [strong].
**One new thing to learn:** generalizing golf's fixed two-tier `label_basis` (video vs. sheet) into a caller-defined ordered set of evidence tiers, so the library isn't hardcoded to golf's specific label sources.
**Why it's worth it:** this is a small, bounded, already-proven mechanism — it just caught two real false claims in this person's own repo — packaged the same way `ledger-tieout` was: a standalone, dependency-light PyPI library shipped once via a secretless release pipeline, addressing a failure mode common to any ML project measuring itself against partial ground truth.
**First step today:** extract `score.py`'s `label_basis` check into a standalone module with a generic `tiers` parameter instead of golf's hardcoded video/sheet distinction, and re-run it against golf's own `labels.json` to confirm identical output.

### 3. Benchmark Tournament Orchestrator — [Experience]
- competence: unlabeled
- labeled_at: —

**Idea:** Generalize poker-llm's tournament orchestration — a 10-level blind-escalation structure with elimination tracking, plus head-to-head/round-robin/knockout comparison modes — out of its poker-specific state into a reusable benchmark-orchestration library for pitting any set of LLMs or agents against each other under a structured competitive format, not just poker.
**Leverages:** Game-engine implementation + multi-LLM benchmarking harness [emerging] (poker-llm) — the orchestration layer (bracket/elimination logic, escalating-difficulty rounds) is generic tournament structure, separable from poker's rules engine.
**One new thing to learn:** decoupling the tournament state machine from poker-specific concepts (blinds, chip stacks, hands) into a generic "match" abstraction — a caller-supplied win/loss/score function and an elimination threshold — so the same orchestrator can run any turn-based or scored competition.
**Why it's worth it:** poker-llm already proved every piece of this works end to end (blind escalation, elimination, three comparison formats, a FastAPI backend and dashboard to review results); building the generalized version hardens a genuinely reusable "benchmark harness design" capability the poker-specific version doesn't yet demonstrate, and it directly serves this person's own repeated need to benchmark models against each other (already done twice — finance_bot's model comparison, poker-llm's LLM matches).
**First step today:** extract poker-llm's blind-escalation/elimination tracker out of `game_management/` into a standalone module driven by a generic `score_match(a, b) -> winner` callback instead of a poker hand result, and run it against a trivial two-player coin-flip stand-in to confirm the bracket logic works independent of poker.

### 4. RLC Second-Order Extension — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Push circuit-simulator's solver one step past its already-shipped single-capacitor RC reach into "signal integrity": add an inductor's companion-model stamp and solve a second-order RLC step response (or a frequency sweep for resonance), the first time the solver would handle two energy-storage elements together rather than one.
**Leverages:** Circuit analysis [strong] — the same backward-Euler companion-model stamping approach already proven for the capacitor extension fits an inductor too (a companion resistance plus a history-current or history-voltage source), reusing the existing MNA stamp framework rather than a new solver architecture.
**One new thing to learn:** the numerics of a genuinely second-order system — critically-/under-/over-damped step response, or an AC frequency sweep for a resonance peak — one well-defined step past the single-capacitor case, not a full SPICE-grade AC engine.
**Why it's worth it:** signal integrity is still named in Direction & Intent with only a single-capacitor RC step response shipped against it as an idea so far; a second reactive element and real damping/resonance behavior is the next concrete, bounded rung on that same ladder, reusing the stamp-based architecture and companion-model technique already validated rather than starting over.
**First step today:** add an `Inductor` component with a backward-Euler companion-model stamp (a series resistance of `L/dt` plus a history-current source from the previous timestep) and solve one fixed series RLC circuit's step response over enough timesteps to see its damping behavior, checking it against the known analytic underdamped/overdamped case.

<!-- prompt-version: 2026-08-14.1 -->
