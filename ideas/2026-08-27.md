# Daily Ideas — 2026-08-27

Sourced from `profile.md` (`last_refresh: 2026-08-23`, unchanged since). Anchors this week's newest
shipped material rather than repeating prior angles: golf's confidence-based pose-quality gate and
five-loop `pose_pipeline.py` consolidation (never yet abstracted as its own reusable artifact,
distinct from golf's earlier measurement/localization work already mined repeatedly); the merge
commit (`2b05186`) that silently dropped a documented CLAUDE.md guardrail carve-out (a real bug
found by hand this week, not yet turned into a tool that would catch the next one); and
stripe-reconciler's now-*spent* OAuth grant, verified end-to-end against live Stripe and catching an
invalid manifest permission (`bank_account_read`) — distinct from the credential-usage chokepoint
pattern already proposed 2026-08-17. The circuit-simulator visualizer is deliberately not re-pitched
a sixth time (five dead pitches through 2026-08-20, a sixth on 2026-08-24, still zero commits since
2026-08-14; nothing has changed to justify another). Today's three strongest ideas land as
[Leverage]/[Leverage]/[Passive] — all direct extractions of material that shipped or was fixed this
week — with a [Stretch] pick against Direction & Intent's explicit "UART/SPI/I2C" line rounding out
the set. No [Experience] idea this round: a strong one would need an idea that's genuinely
open-ended for this person rather than a known extraction — e.g., actually wiring the UART decoder
below against a real captured signal (oscilloscope/logic analyzer) instead of a synthetic bitstream,
which would harden a first-time hardware-measurement capability rather than just produce a reference
artifact.

### 1. Pose-Quality-Gate Library — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract golf's confidence-based per-landmark pose gate and consolidated `pose_pipeline.py`/`run_pose_detection()` loop into a standalone, landmark-set-agnostic Python package that any MediaPipe/ML-Kit-based pose app can drop in to reject low-confidence frames before analysis.
**Leverages:** Computer vision on real footage [strong] plus this week's specific golf work — a `confidenceFloor` (0.5) gate checked against 7 required landmarks, and a single `run_pose_detection()` module replacing five independently-maintained copies of the same detection loop.
**One new thing to learn:** generalizing a fixed 7-landmark, golf-specific gate into a configurable API (arbitrary landmark sets, pluggable confidence source) packaged for PyPI, rather than one more internal module.
**Why it's worth it:** this is a pure extraction of a bug class just solved internally — every pose-based fitness/PT/sports app hits the same "garbage-in frame" problem — and the ledger-core-out-of-stripe-reconciler playbook already proved this exact move (decouple a hardened internal module, publish standalone) works.
**First step today:** rewrite `run_pose_detection()`'s landmark check to accept an arbitrary list of required landmark indices instead of golf's fixed 7, as the first library-ready function signature.

### 2. Guardrail-Text Merge Watchdog — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** A GitHub Action that snapshots anchor-delimited "protected" doc snippets (e.g. `CLAUDE.md` guardrail sections) and fails a PR if a merge would silently drop or alter one — the exact failure mode merge `2b05186` produced this week when it dropped a documented exception carve-out with the surrounding code still correct.
**Leverages:** Design documentation as a first-class artifact [strong] plus CI/CD and scheduled automation [strong].
**One new thing to learn:** publishing a composite GitHub Action to the Marketplace (`action.yml`, versioned releases) rather than a one-repo script.
**Why it's worth it:** this bug class was just found by hand, costs nothing to re-check once written, and generalizes to any team keeping load-bearing prose next to code — install once, runs free on every PR forever, no per-customer effort.
**First step today:** define the anchor-comment convention (`<!-- guardrail:start id=X -->` / `:end`) and write the ~30-line script that diffs those blocks between a merge's two parents and its result.

### 3. Stripe Manifest Reality-Check — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** A CLI for other Stripe Apps developers that cross-checks a manifest's declared permissions against Stripe's real, published grant list and against what the code actually calls — catching an invalid entry (like this week's `bank_account_read`, which doesn't exist) or a declared-but-unused scope before it reaches production.
**Leverages:** Applied web application security [strong] — this week's live-Stripe-verified manifest correction and the `stripe_auth()` credential chokepoint, now proven end-to-end against production rather than only CI's stub.
**One new thing to learn:** packaging the one-off manual audit as a distributable tool that takes an arbitrary `manifest.json`, not a fixed internal one.
**Why it's worth it:** this exact defect (an invalid permission silently downgrading a confirmation screen's real strength) was just caught in one's own live app; no equivalent pre-launch linter exists for Stripe Apps developers today, and it's a sellable one-time checklist tool, not new engineering.
**First step today:** script this week's manual manifest-vs-grant-list comparison as a function that takes any manifest file plus Stripe's permission list and prints the invalid/unused diff.

### 4. Serial Protocol Decoder & Waveform Toolkit — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Build a small Python toolkit that decodes a captured UART/SPI/I2C bitstream into structured messages and renders a timing-diagram waveform, sold once as a paid reference/teaching toolkit for hobbyist embedded learners.
**Leverages:** Circuit analysis [strong]'s rigor with real signal semantics (exact stamp/timing correctness, not approximation) plus [conceptual] hardware–software boundary reasoning and hands-on protocol-capture experience (Wireshark/TCP handshake analysis) — decoding a structured bitstream into a labeled timeline is the same shape whether the frames are TCP or I2C.
**One new thing to learn:** the bit-level timing rules of one serial protocol family (start/stop bits, clock polarity/phase, ack/nack) — genuinely new, scoped to one protocol.
**Why it's worth it:** this is the next explicit line in Direction & Intent ("UART/SPI/I2C") and produces exactly the documented waveform/timing-diagram portfolio artifact the hardware track calls for, without requiring the ten other unstarted hardware items at once.
**First step today:** pick UART (simplest framing) and write a decoder for one fixed-baud-rate synthetic bitstream, printing the decoded bytes.
