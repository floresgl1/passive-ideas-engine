# Daily Ideas — 2026-09-05

Sourced from `profile.md` (`last_refresh: 2026-09-04` — new since yesterday's digest, which still
read `2026-08-30` because the weekly refresh landed after that run). The refresh added one new
project, **studyprof** (a FastAPI study/quiz backend, first shipped instance of a "document
ingestion / file-upload text extraction" capability anywhere in this profile — PyMuPDF PDF text
extraction wired end to end through `POST /upload/`), a new `[emerging]` skill of the same name, and
a third proven instance of REST API design. None of that material has anchored a digest before today
— this is its first appearance. Today's four ideas draw on it directly: the shipped PDF-extraction
endpoint itself, and the project's own honest gap (a `generate_question()` helper that calls a
Nebius-hosted, OpenAI-SDK-compatible model but isn't wired to any route yet — real code, unreachable,
unverified). Rounding out the spread, one piece of golf material sits in the profile since its earliest
description and has never itself anchored an idea: "correct math (circular smoothing, pixel-space
angles)" — the wraparound-safe angle handling underneath every one of golf's already-mined phase and
orientation checks, but never itself the core. Poker-llm's tournament layer, ledger-tieout's adapter
architecture, the Stripe OAuth chokepoint, memory-hierarchy visualizers (mined twice already), the
Iron Law/ALU line, and the sub-agent template-pack material all stay untouched again today, all mined
repeatedly already. Today's four land as a full [Passive]/[Stretch]/[Leverage]/[Experience] spread,
none weakened to force it. No idea crosses the keystone — the visual/JS circuit-simulator direction
stays closed, and nothing below re-touches it.

### 1. Document Text-Extraction API — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** Generalize studyprof's `POST /upload/` → `extract_text()` PyMuPDF pipeline out of its quiz-app-specific context into a small, standalone paid API: accept an uploaded PDF (later: docx/pptx), return clean extracted text and per-page metadata, billed per call or per seat.
**Leverages:** REST API design [strong] (studyprof is a third proven instance, alongside stripe-reconciler and visual-search-engine) plus Document ingestion / file-upload text extraction [emerging] — the first shipped instance of this capability anywhere in the profile.
**One new thing to learn:** hardening a file-upload endpoint for public, untrusted traffic — size limits, MIME/type validation, and malformed-PDF handling — none of which studyprof's single-user internal endpoint currently needs to do.
**Why it's worth it:** document-to-text extraction is a small, well-understood, repeatedly-purchased utility (RAG pipelines, note-taking tools, other indie developers who don't want to wire up PyMuPDF themselves) — exactly the "paid API sold repeatedly" shape, and the core extraction logic is already proven working code, not a cold start.
**First step today:** pull `extract_text()` out of studyprof into a standalone FastAPI service with its own route, and add one validation check (reject non-PDF content-type) that studyprof's internal-only version never needed.

### 2. Upload-to-Quiz Generator — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Finish wiring studyprof's dormant `generate_question()` helper — written, but not connected to any FastAPI route, and never exercised — into a live endpoint that takes an uploaded document, extracts its text, and returns LLM-generated quiz questions end to end, then package that as a small subscription tool for students/teachers building study guides from lecture notes or textbook chapters.
**Leverages:** LLM agent engineering [strong] (bounded steps, retry budgets, enumerated error handling — the same discipline already proven in finance_bot) and Document ingestion / file-upload text extraction [emerging], both real capabilities; the specific application here is new and unverified, which is why this stays a Stretch rather than a Leverage/Passive idea presented as ready.
**One new thing to learn:** productizing a third-party, OpenAI-SDK-compatible chat completion call (Nebius's `Nemotron-3.5-Lightning` host) that has never been exercised against a live route — adding real error handling for a dependency this profile has zero operating history with, not the ten new things a full alternate LLM-vendor integration would require.
**Why it's worth it:** this is the single highest-leverage next step for studyprof specifically — the code for both halves (extraction, question generation) already exists in the repo but has never been connected or tested together; closing that one gap turns an inert side project into a shippable product, and quiz/study-guide generation from uploaded materials is a proven willingness-to-pay category.
**First step today:** write the missing route that calls `extract_text()` then `generate_question()` in sequence on a real uploaded PDF, and manually verify the Nebius call actually returns a usable question — the one thing that has never been checked in this repo.

### 3. Circular-Angle Math Library — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract golf's "circular smoothing, pixel-space angles" math — correct handling of angle wraparound (e.g. averaging or smoothing a sequence of angles near the 0°/360° boundary without the naive-mean bug) plus the pixel-space-vs-true-angle distinction — into a small, standalone, dependency-light Python utility library any CV/motion-tracking project can import instead of re-deriving.
**Leverages:** Computer vision on real footage [strong] — this specific math is cited in golf's own capability description as part of what makes its measurement pipeline defensible, but it has never itself been the anchor of an idea (only the confidence gate, dedup, and left-handed pieces built around it have been mined so far).
**One new thing to learn:** generalizing golf's specific wraparound cases (hardcoded to its own joint angles) into a small set of caller-agnostic primitives — `circular_mean`, `circular_smooth(window)` — that work for any angle sequence, not just golf's.
**Why it's worth it:** angle-wraparound bugs are a well-known, recurring silent-failure class in any pose/motion/robotics pipeline (a joint angle crossing 359°→0° silently corrupts a naive moving average), this person has already solved it correctly once under real conditions, and packaging it the same way `ledger-tieout` was — small, zero/low-dependency, PyPI, sold once and reused indefinitely — is a fast, low-risk ship.
**First step today:** pull golf's circular-smoothing function out of its joint-angle-specific context into a standalone `circular_mean(angles)` function with no golf dependencies, and write one test that a naive arithmetic mean would fail (a wraparound case around 0°/360°) but this one passes.

### 4. Zero-to-CI Starter Kit — [Experience]
- competence: unlabeled
- labeled_at: —

**Idea:** Build a drop-in template that bootstraps pytest, a basic test suite skeleton, and a fail-closed GitHub Actions CI workflow onto an existing FastAPI + SQLAlchemy + Postgres backend that currently has neither — using studyprof's own real, named gap (six commits, all pushed directly to `main`, no tests anywhere, no CI) as the concrete reference case rather than a hypothetical one.
**Leverages:** CI/CD and scheduled automation [strong] and Testing discipline [strong] (finance_bot, stripe-reconciler, golf) — the same fail-closed-when-no-suites-are-found pattern already proven in stripe-reconciler, applied here to a stack (FastAPI + SQLAlchemy + Postgres) this person has just shipped a second real instance of.
**One new thing to learn:** making the template stack-detecting rather than studyprof-specific — reading a target repo's `pyproject.toml`/`requirements` to generate the right fixture (e.g. a test Postgres container vs. SQLite) instead of hardcoding studyprof's own dependencies.
**Why it's worth it:** "shipped fast, zero tests, zero CI, straight to main" is a common and named failure mode for solo FastAPI backends, not just this one — and building the generalized fixture-plus-CI template against a real, currently-untested repo hardens exactly the gap studyprof itself has open, while producing a sellable, reusable template rather than a one-off internal fix.
**First step today:** write one smoke test for studyprof's `POST /upload/` route (the endpoint this refresh's own capability write-up is built around) against a throwaway test Postgres instance, and wire it into a minimal GitHub Actions workflow that fails if the suite doesn't run — the first CI check this repo has ever had.

<!-- prompt-version: 2026-08-14.1 -->
