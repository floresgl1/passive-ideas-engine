# Daily Ideas — 2026-08-14

Sourced from `profile.md` (`last_refresh: 2026-08-13`). Yesterday's digest ran while
`profile.md` still read `last_refresh: 2026-08-11` and explicitly noted "no new shipped work has
landed since 2026-08-12's refresh" — but the 2026-08-13 weekly-refresh commit landed *after*
that digest ran, so today is the first pass to react to it. Three pieces of fresh signal: (1)
`ledger-core` is a new project entry — stripe-reconciler's double-entry discipline decoupled into
a standalone, zero-dependency, 577-tests-green library, with one honest open gap (`pip install
ledger-core` is still aspirational, never published); (2) two skills graduated to `[strong]` on
second proof points — **relational/double-entry data modeling** (now proven twice: stripe-
reconciler and ledger-core) and **AI-assisted development workflow at the tooling level** (now
proven twice: golf and finance_bot's new `daily-run-triage` sub-agent, which caught a real 5-day
silent production bug — a trading-guard flag mis-stamped by a market-closed no-op); (3)
circuit-simulator's `current-sources` branch — four controlled/dependent sources (VCVS/VCCS/CCVS/
CCCS), a SPICE-style netlist reader, a CLI, and ill-conditioned-circuit rejection — exists but is
explicitly "not yet credited," unmerged to `main`. Today's four ideas react directly to these
three deltas instead of reworking ground eleven prior digests already covered.

## Step 1 — Abstracted capabilities (from [strong]/[emerging] only)

- Relational/double-entry data modeling, now `[strong]` on a second independent proof — integer
  cents, explicit Suspense account, idempotent constraints, and (new, via ledger-core) a
  currency guard plus property-based zero-sum testing, generalized out of any one payment
  processor.
- AI-assisted development workflow at the tooling level, now `[strong]` on a second independent
  proof — the specific "enforced read-only scope, forbidden from fixing what it finds" pattern,
  shown in golf and now in finance_bot's `daily-run-triage`, which surfaced a real bug rather than
  a hypothetical one.
- Numerical circuit analysis as a callable engine (`[emerging]`) — the MNA solver on `main` plus,
  newly visible this refresh, an unmerged branch carrying dependent-source support and a netlist
  CLI, which is real progress toward the keystone's named gap even though it isn't shipped yet.
- Production observability and silent-failure handling (`[strong]`) — pre-flight validation and
  staleness detection, now with a concrete result attached: a diagnostic sub-agent that found a
  bug that had been silently skipping live trades since 2026-08-07.

## Step 2–3 — Ideas

### 1. Ledger-Core Reconciliation Kit — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Finish publishing `ledger-core` to PyPI — closing the one honest gap `profile.md`
itself names — and package it as a CLI trial-balance / suspense-account reconciliation tool sold
as a one-time license to small bookkeeping shops and freelance accountants who need to confirm
any CSV of transactions actually balances to zero without adopting a full SaaS.
**Leverages:** ledger-core's already-built, zero-dependency, currency-guarded double-entry engine
(577 tests green) plus the design-doc-first, fail-closed CI discipline now proven twice
(stripe-reconciler, ledger-core).
**One new thing to learn:** Real package distribution — registering on PyPI, versioning, and
confirming a clean `pip install ledger-core` from a fresh environment, the exact step
`profile.md` flags as still aspirational.
**Why it's worth it:** The hard part — the engine, the tests, the currency guard — is already
done and green; publishing it is the only step between "internal library" and a sellable asset,
so the leverage ratio here is unusually high.
**First step today:** Run `python -m build` and `twine check` against ledger-core's existing
`pyproject.toml`, upload to TestPyPI, and do one clean `pip install` from a scratch virtualenv to
confirm the README's install line actually works.

### 2. Silent-Failure Triage Pack — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** Package the specific "read-only, forbidden-from-fixing, report-only" sub-agent pattern
— now proven twice, in golf and in finance_bot's `daily-run-triage` — as a small library of
pre-written Claude Code sub-agent configs targeting common silent-failure shapes in scheduled
automations (a cron job that exits 0 without doing its job, a guard flag stamped by the wrong
code path, stale data masquerading as fresh), sold as a one-time template pack to other solo
developers running unattended jobs.
**Leverages:** AI-assisted development workflow at the tooling level, freshly promoted to
`[strong]` on a second independent proof, plus the production-observability discipline
(pre-flight validation, staleness detection) from finance_bot.
**One new thing to learn:** Generalizing `daily-run-triage`'s specific check (a market-closed
no-op mis-stamping a guard flag) into a small catalog of domain-agnostic silent-failure
signatures a stranger's cron job could exhibit, each with its own enforced-scope sub-agent
template.
**Why it's worth it:** This bug was real, not hypothetical — a sub-agent scoped to
Bash/Read/Grep/Glob only, banned from fixing anything, found a production bug that had been
silently skipping trades for 5 days; that is a demonstrable result to package, not a speculative
pitch.
**First step today:** Write out `daily-run-triage`'s actual scope file and hard-rule list with all
finance_bot-specific field names stripped out, and confirm the generalized version still reads as
a complete, enforceable spec on its own.

### 3. Dependent-Source SPICE CLI — [Experience]
- competence: no knowledge
- labeled_at: 2026-08-17

**Idea:** Merge the unmerged `current-sources` branch — four controlled/dependent sources
(VCVS/VCCS/CCVS/CCCS), a SPICE-style netlist reader, a CLI, and ill-conditioned-circuit rejection
— into circuit-simulator's `main`, add tests confirming it holds under the same rigor as the
existing 26, and package the CLI as a small paid PyPI tool for EE students and hobbyists who want
a lightweight SPICE-like solver without installing full SPICE.
**Leverages:** The already-written (if not yet merged) MNA solver extension, plus the packaging
discipline already proven in ledger-core (`pyproject.toml`, fail-closed CI across Python
versions).
**One new thing to learn:** CLI ergonomics for a netlist-driven tool — argument parsing, file-
based input, and clear error surfacing for the existing `SingularCircuitError`/ill-conditioned
rejection — distinct from the JSON/hosted-API shape a prior digest explored for the plain
resistor/voltage-source solver.
**Why it's worth it:** This is literally the "no dependent sources" gap `profile.md`'s own
Keystone note names as unresolved, and the code already exists — the remaining work is merging,
testing, and packaging, not new invention.
**First step today:** Diff the `current-sources` branch against `main`, resolve any conflicts,
and re-run the full test suite locally to confirm all tests — old and new — pass together before
opening the merge.

**Keystone:** crosses the keystone — merging and shipping dependent sources resolves the specific
gap `profile.md` names as still open on the hardware/architecture axis, beyond the "solver
exists" partial crossing already reached.

### 4. Scoped Sub-Agent Engineering Course — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Turn the AI-assisted development workflow discipline — enforced tool scopes, explicit
hard rules, read-only diagnostic sub-agents, and the habit of treating a sub-agent's output as a
measurement rather than a fix — into a short, sold-once video/written course for developers
building their own unattended automations, using the `daily-run-triage` 5-day-bug story as the
concrete teaching case.
**Leverages:** AI-assisted development workflow `[strong]`, plus the epistemic-discipline
meta-skill used explicitly as a teaching angle — its own guardrail names teaching as the correct
use, never a standalone product.
**One new thing to learn:** Structuring and producing instructional content — course/video
scripting, pacing a technical narrative for people who haven't seen the repos — a genuinely new
skill distinct from any code written so far.
**Why it's worth it:** This is a content asset, sold repeatedly with no per-buyer marginal work
once recorded, and it's the one idea today that uses the epistemic through-line the way its own
guardrail permits — as a teaching wedge, not a product in itself.
**First step today:** Outline the course's single core module — "scope a sub-agent so it can only
diagnose, never fix" — using the actual `daily-run-triage` config and the 5-day bug as the worked
example, and time how long a five-minute walkthrough of it actually takes to script.

## Category spread
Leverage, Passive, Experience, Stretch — one of each, driven entirely by the three-part delta in
the 2026-08-13 profile refresh (ledger-core, the newly-`[strong]` AI-assisted-dev-workflow skill,
and the previously-uncredited `current-sources` branch) that yesterday's digest ran too early to
see.

## Filters check
All four fit 2–6 weeks part-time (idea 1's first step is a TestPyPI upload of code that already
passes 577 tests; idea 3's first step is a branch diff and test re-run, not new solver math). All
rest on infrastructure or reasoning already shipped or already written (ledger-core's engine, the
daily-run-triage config, the current-sources branch's controlled sources) rather than freelancing
or hourly work — a licensed CLI kit, a sold template pack, a packaged CLI tool, and a sold course
are each repeatable, not per-hour. Each new-thing-to-learn is one focused piece — PyPI packaging,
a failure-signature catalog, netlist CLI ergonomics, or course scripting — not a curriculum.
