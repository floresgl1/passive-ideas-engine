# Daily Ideas — 2026-08-28

Sourced from `profile.md` (`last_refresh: 2026-08-23`, unchanged for the fourth consecutive digest —
no new weekly refresh has landed). The last two digests (08-24, 08-27) already extracted the
freshest "this week" material — golf's pose-quality gate, the `2b05186` guardrail-drop bug, and
stripe-reconciler's now-spent OAuth grant with its manifest fix — so today deliberately reaches for
four things that have never anchored a digest before, checked against the full `ideas/` history
rather than just the prior day: golf's `--left` mirroring flag (threaded through five CLI scripts
this week, including the resolved ±90° fold-boundary ambiguity), which so far has only ever been a
supporting detail inside golf's own project description, never its own idea; visual-search-engine's
CLIP/ChromaDB stack paired for the first time with finance_bot's alerting/observability discipline
(prior visual-search-engine pitches were a text/RAG generalization, a video-search extension, and a
React UI — never an alerting product); finance_bot's statistical-validity toolkit (CI lower bound
vs. breakeven, hit-rate-needs-payoff-ratio) applied outside finance for the first time, closing the
loop on golf's own documented eyeballing failure (`locate_swing` scored 2/10 once real labels
existed) rather than just restating the finance use; and computer-architecture's Iron Law /
quantitative-tradeoff reasoning extended from datapath choices (already used once, 08-19's pipeline
hazard visualizer) to memory-hierarchy analysis, a distinct sub-topic never picked before. The
circuit-simulator visualizer is again not proposed — six-plus prior pitches, zero commits since
2026-08-14 — and neither is the Ruiz-equilibration ill-conditioning check or the epoch-seam
provisioning pattern, both already fully mined (2026-08-19 and 2026-08-12/08-20 respectively).
Today's four land as [Leverage]/[Passive]/[Experience]/[Stretch] — a full spread, none weakened to
force it. No idea crosses the keystone today; nothing here touches the visual/JS circuit-simulator
axis.

### 1. Bilateral Pose-Orientation Library — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract golf's `--left` mirroring flag — threaded through five CLI scripts this week, including the resolved ±90° fold-boundary ambiguity (folds to +90 regardless of point order) — into a standalone orientation-normalization stage that any bilateral-movement pose pipeline (batting, tennis, boxing form checkers, physical-therapy exercise apps) can drop in to correctly support left- and right-sided users from one landmark set.
**Leverages:** Computer vision on real footage [strong], specifically this week's handedness support and the just-resolved boundary-fold edge case, distinct from the confidence-gate library already extracted 2026-08-27 — a different bug class (orientation, not signal quality) for the same buyer base.
**One new thing to learn:** generalizing golf's fixed swing-plane mirroring math into a configurable API that accepts an arbitrary landmark topology instead of golf's specific joint set.
**Why it's worth it:** the hard part — correctly resolving the boundary case where naive mirroring breaks — was just solved and verified this week; every bilateral-movement pose app hits the same problem, and this is a pure extraction, not new engineering.
**First step today:** rewrite golf's `_fold` boundary function to take a landmark-index mapping as a parameter instead of golf's hardcoded joint indices, as the library's first configurable entry point.

### 2. VisualWatch Alerts — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** A hosted service that lets a user save a text or reference-image query against a live-updating image feed (a marketplace category, a resale/collectibles search, a public webcam folder) and fires a Discord/webhook alert the moment a new image clears a similarity threshold — visual-search-engine's CLIP embed/store/query loop combined with finance_bot's threshold-based, staleness-aware alerting for the first time.
**Leverages:** Embeddings and vector retrieval [emerging] (visual-search-engine's CLIP/ChromaDB/FastAPI stack) plus Production observability and failure handling [strong] (finance_bot's staleness detection and transition-only alerting, so a matching image alerts once, not on every poll).
**One new thing to learn:** polling/ingesting a live external image source on a schedule and diffing against previously-seen items — the embedding, storage, and similarity-query layers are already built and running.
**Why it's worth it:** both halves already exist and work in isolation; niche resale/collectible watchers already pay for keyword-alert tools, and none of them do actual visual similarity matching, which is the one thing this stack already does well.
**First step today:** write a script that polls one public image listing page on a fixed interval, embeds each new image with the existing CLIP pipeline, and prints a match when it clears a similarity floor against one saved reference image.

### 3. Small-Sample Validity Checker — [Experience]
- competence: unlabeled
- labeled_at: —

**Idea:** Package finance_bot's statistical-validity toolkit (confidence-interval lower bound vs. a computed breakeven, hit-rate-needs-payoff-ratio, sample-size intuition) as a general-purpose "did this actually work" checker for any small-sample before/after claim, and prove it outside finance for the first time by running it against golf's own drill-effectiveness data — the same project whose eyeballed "locate_swing is better" claim was later falsified 2/10 once real labels existed.
**Leverages:** Applied statistical reasoning under uncertainty [strong], applied here for the first time outside finance_bot, which is exactly the second-domain proof this capability has never had.
**One new thing to learn:** reframing the CI-lower-bound-vs-breakeven test for a non-trading claim shape (before/after drill scores instead of trade P&L) — one reframing, not a new statistical method.
**Why it's worth it:** this closes a real, documented gap in golf's own process (a fix eyeballed as working was later refuted by data) using a tool already proven to gate live decisions in finance_bot, and the same "I eyeballed an improvement" mistake is common enough elsewhere (habit trackers, coaching, course creators) to sell as a small paid checklist/notebook template once validated on real data.
**First step today:** run the CI-lower-bound-vs-breakeven test against golf's existing pre/post drill-effectiveness numbers, using whatever session data already exists, and record whether the claimed improvement actually clears the bar.

### 4. Memory-Hierarchy AMAT Visualizer — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Build a small tool that simulates direct-mapped and set-associative cache behavior over a given memory-access trace, computes hit/miss rates and average memory access time (AMAT), and visualizes the Iron-Law tradeoff as associativity/block-size change — packaged as a licensable portfolio/teaching widget for hardware-track learners, the same scoping already proven with the EDF/Rate-Monotonic schedulability widget.
**Leverages:** Design documentation as a first-class artifact [strong] and CI/CD-adjacent build discipline, applied to `[conceptual]` computer-architecture datapath & tradeoff reasoning (Iron Law, quantitative tradeoff analysis already used once to reject an ALU multiplier) — extended here to a second architecture sub-topic, memory hierarchy, never picked before.
**One new thing to learn:** implementing cache indexing/tag/replacement-policy simulation and AMAT computation correctly — one well-defined algorithm family, not a curriculum.
**Why it's worth it:** it reaches the same unshipped hardware-track direction the keystone note flags as the recruiting-relevant axis, at the same small scope that already worked once (EDF widget, 2026-08-07), without requiring FPGA tooling or ten new concepts at once.
**First step today:** implement a direct-mapped cache simulator that takes a fixed synthetic address trace and prints hit/miss counts, as the smallest slice that proves the core mechanic before adding associativity or visualization.
