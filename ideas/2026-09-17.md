# Daily Ideas — 2026-09-17

### 1. Desk-Ergonomics Movement Scorer — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** A webcam-based desk-ergonomics tool: record ~30 seconds of yourself at your desk and get a report of neck/shoulder/wrist angle deviations from neutral posture with concrete corrective adjustments, sold as a one-time-purchase self-serve assessment to remote workers and small companies who'd otherwise pay for an in-person ergonomic evaluation.
**Leverages:** Computer vision on real footage [strong] — the fully proven, dual-language pose-estimation pipeline (physically-reasoned landmark choice, scale-invariant normalization, confidence gating) built for golf, applied to a new domain — plus Epistemic discipline (report angle deviations as measurements with drills, never a medical verdict) and Testing discipline [strong].
**One new thing to learn:** a new landmark-to-angle mapping for desk posture (neck flexion, shoulder elevation, wrist deviation) in place of golf's swing-phase landmarks — new domain knowledge layered on an already-shipped, already-tested measurement pipeline, not new ML capability.
**Why it's worth it:** reuses a hardened CV pipeline wholesale into a much larger, non-overlapping market (WFH ergonomics/RSI prevention) with no dependency on the trading or golf verticals — a bounded 2–6 week domain port, not a new build from scratch.

### 2. Agent Contract Tests (pytest plugin) — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** A pytest plugin (`pytest-agent-contract`) that lets developers write assertions against an LLM agent's tool-call trace — expected call order, enumerated allowed verdict values, step-count ceilings, retry-budget compliance — and fails CI when an agent pipeline violates its own declared contract, the same way a type system catches a code regression.
**Leverages:** LLM agent engineering [strong] — finance_bot's bounded-ReAct architecture (step caps, retry budgets, enumerated error/verdict vocabularies) now proven reusable unchanged at two separate integration points — plus Testing discipline [strong] (golden-fixture, characterization-test habits proven across three repos).
**One new thing to learn:** designing a general assertion DSL that inspects an arbitrary caller's own agent trace, rather than hand-testing one specific agent's outputs inline, and packaging it as an installable pytest plugin.
**Why it's worth it:** almost no solo/small team wiring LLM agents into real pipelines has CI-gated behavioral tests for the agent layer itself, and this person has already built and proven that exact bounded-agent discipline twice in production — a small, genuinely new dev-tool product that complements rather than competes with the runtime-diagnostic template pack currently in build.

`last_refresh` still reads 2026-09-14, unchanged since the last digest, which already extracted the fresh finance_bot/ops-subagent-templates material directly (model promotion gate, fill-safety wrapper, decision-point scaffold) — today switches to invention mode per the staleness check, proposing two new, unmined ventures instead of re-mining the same window.
