# Daily Ideas — 2026-09-21

### 1. Self-Hosted Transcription Sidecar — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** A Docker-packaged, self-hosted speech-to-text microservice (wraps `faster-whisper`, CPU/int8) that indie developers drop into their own app instead of paying per-minute for a hosted STT API, with the API-key-scoped session-isolation pattern already proven in voice-quiz built in from the start.
**Leverages:** Self-hosted ML inference serving [emerging] — voice-quiz's `faster-whisper` Docker deployment (model pre-downloaded at build time, persistent-disk cache) — plus Document/content ingestion discipline [strong] and Containerization/deployment [emerging].
**One new thing to learn:** generalizing the transcription service into a standalone, app-agnostic HTTP microservice with its own multi-tenant API-key model, instead of routes embedded inside one app's own FastAPI backend.
**Why it's worth it:** per-minute hosted transcription pricing eats margin on any voice feature; this exact serving pattern is already working, verified code, and packaging it once serves many buyers — the same "productize a proven internal pattern" shape already validated as sellable by the current build, but in audio infrastructure rather than ops tooling, so it doesn't compete with it.

### 2. Multi-Currency Ledger Export Kit — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Extend the published `ledger-tieout` PyPI package with the currency-formatting and accounting-export logic stripe-reconciler just proved out (per-currency minor-unit tables, per-client accounting-software tax-rate-name mapping) into a standalone add-on other bookkeeping/reconciliation tools can install.
**Leverages:** Relational/double-entry data modeling [strong] — an already-published, zero-dependency, design-doc-first PyPI package (`ledger-core`) with a secretless OIDC release pipeline already in place.
**One new thing to learn:** generalizing stripe-reconciler's Stripe-specific, per-client tax-rate-name config into a caller-supplied export-target mapping so it isn't tied to one payment processor.
**Why it's worth it:** currency-formatting bugs (JPY rendered at 100x) and hardcoded US-only tax-rate names are exactly the edge cases most solo bookkeeping tools silently get wrong, and this person just found and fixed both firsthand — selling the verified fix as a library extension is a small repackage of a rare, proven capability, not new engineering.

### 3. React Refactor Fingerprint Harness — [Experience]
- competence: unlabeled
- labeled_at: —

**Idea:** Generalize the headless-Chrome state-fingerprinting harness built to verify stripe-reconciler's `App.jsx` split (script a set of interactions, snapshot rendered DOM/state before and after a refactor, diff automatically) into a standalone CLI any React developer can point at their own app before merging a large refactor.
**Leverages:** Testing discipline [strong] and React 19 + Vite SPA experience [emerging] — the harness already caught a real bug in its own comparison script during actual use, so it's proven, not theoretical.
**One new thing to learn:** making the interaction script and diffing logic app-agnostic via a small config/selector API, instead of hardcoded to stripe-reconciler's own routes and component tree.
**Why it's worth it:** "did this refactor change behavior" is a fear every non-trivial React codebase has, usually answered with manual QA; this person already built and validated the exact mechanism that answers it automatically, so most of the risk is retired before a new line of code is written.
