# Daily Ideas — 2026-09-06

Sourced from `profile.md` (`last_refresh: 2026-09-04`, unchanged for the third consecutive digest).
The last two digests off this refresh already pulled studyprof's extraction endpoint, its dormant
quiz-generation helper, its untested/no-CI gap, golf's circular-angle math, the reinstated backswing-
must-outlast-downswing check, the `label_basis` provenance mechanism, poker-llm's tournament
orchestration layer, and a second reactive element (inductor/RLC) for circuit-simulator — all left
untouched today. Today instead reaches for four pieces of material checked against the full `ideas/`
history and never used as an idea's core before: stripe-reconciler's real `STRIPE_CLIENT_ID` vs.
`STRIPE_APP_CLIENT_ID` env-var drift (a reported `l`/`1` typo on a second, redundant variable,
implicated in a prior App Review rejection and only now consolidated to one source of truth) — cited
before only as a supporting detail in others' review, never itself the anchor; golf's three quieter
robustness fixes shipped this week (an explicit `numpy` pin, an empty-video crash guard in
`phase_montage.py`, and `detect_phases` returning `None` instead of crashing on a too-short trajectory)
— distinct from the confidence-gate and physical-plausibility checks already mined from the same
window; finance_bot's Tavily-backed live web search, sitting in the profile's tech list since the
first refresh and never itself the anchor of an idea (the embedding-based "Grounded Support Agent Kit"
from 08-10 draws on visual-search-engine's static vector store instead); and, for the first time, a
pairing between CoCircuit's WebMCP tool-surface pattern and poker-llm's tournament engine — two
already-shipped `[emerging]` capabilities named together only in passing on 09-02, never combined as
an idea's own core. The Iron Law/ALU line, the sub-agent template-pack material, the OAuth chokepoint,
memory-hierarchy visualizers, and ledger-tieout's adapter architecture all stay untouched again today,
all mined repeatedly already. Today's four land as a full [Leverage]/[Passive]/[Experience]/[Stretch]
spread, none weakened to force it. No idea crosses the keystone — the visual/JS circuit-simulator
direction stays closed, and nothing below re-touches it.

### 1. Config Alias Drift Linter — [Leverage]
- competence: unlabeled
- labeled_at: —

**Idea:** Build a small static-analysis script that scans a repo for two or more environment-variable names that plausibly refer to the same secret or config value (near-duplicate names, or names read at different call sites for what should be one source of truth) and fails CI on the pattern — using stripe-reconciler's own real incident as the reference case: a redundant `STRIPE_CLIENT_ID` had drifted from the canonical `STRIPE_APP_CLIENT_ID` by a single `l`/`1` typo, was implicated in a prior App Review rejection, and was only just now removed in favor of one variable used consistently in the authorize URL, token exchange, and client object.
**Leverages:** CI/CD and scheduled automation [strong] and Applied web application security [strong] — the same fail-closed CI discipline already proven in stripe-reconciler and ledger-core, aimed at a config-hygiene bug class rather than a code bug.
**One new thing to learn:** a heuristic for flagging *plausible* env-var aliasing (fuzzy name similarity, or two names whose values are read into the same downstream call) without false-positiving on genuinely distinct variables — a bounded pattern-matching problem, not a new architecture.
**Why it's worth it:** this exact bug class already cost stripe-reconciler a production incident and an App Review rejection cycle before anyone noticed the drift by hand; a small linter that catches it automatically is a drop-in GitHub Action any team managing secrets across multiple env files or deploy targets would install once and run free forever.
**First step today:** write a ~40-line script that lists every `os.environ`/`os.getenv` key referenced across a repo, flags any two keys with a Levenshtein distance below a small threshold or a shared prefix plus differing suffix, and run it against stripe-reconciler's own history to confirm it would have caught `STRIPE_CLIENT_ID`/`STRIPE_APP_CLIENT_ID` before the fix landed.

### 2. Boundary-Condition Fuzzer — [Passive]
- competence: unlabeled
- labeled_at: —

**Idea:** Build a small pytest plugin that automatically generates boundary-shaped inputs — empty sequences, single-element sequences, and sequences shorter than a caller-declared window size — for any numeric/array-processing function under test, and asserts it returns a documented sentinel or raises a named exception rather than crashing with an unhandled one; using golf's own three real fixes this week as the reference cases it would have caught automatically: an explicit `numpy` version pin, an empty-video guard added to `phase_montage.py`, and `detect_phases` changed to return `None` instead of crashing when the trajectory is shorter than the smoothing window.
**Leverages:** Testing discipline [strong] and Computer vision on real footage [strong] — golf's own history of finding and fixing exactly this bug class by hand, one crash at a time, across five independently-maintained detection loops before this week's `pose_pipeline.py` consolidation.
**One new thing to learn:** designing a small, declarative "boundary shape" generator (empty/single-element/shorter-than-window) specific to time-series and array pipelines, distinct from general-purpose property fuzzing — a bounded scope, not a new fuzzing framework.
**Why it's worth it:** three real crashes were found and patched reactively in one repo in a single week; a generic plugin that surfaces this exact bug class automatically, before a user's malformed input does, is a small tool any developer shipping numeric/CV pipelines would install once and rerun on every commit.
**First step today:** write the plugin's first generator — an empty array and an array one element shorter than a given window size — and run it against golf's own (already-fixed) `detect_phases` to confirm it independently reproduces the crash that was found and patched by hand this week.

### 3. Cited Web-Search Agent Kit — [Experience]
- competence: unlabeled
- labeled_at: —

**Idea:** Extract finance_bot's Tavily-backed live web search, wrapped in its existing bounded-agent discipline (step caps, retry budgets, enumerated error categories), into a small template for an LLM agent that must answer only from *live* search results it actually retrieved this run — citing the source for every claim and returning a named "insufficient evidence" verdict rather than answering ungrounded — distinct from the already-shipped "Grounded Support Agent Kit" (08-10), which grounds against a static, pre-embedded vector store rather than a real-time web search call.
**Leverages:** LLM agent engineering [strong] — finance_bot's bounded-step, enumerated-error-category architecture — applied here to a tool this person has already wired into a production agent (Tavily) but never extracted or re-purposed on its own.
**One new thing to learn:** turning a raw Tavily search response into a citation-checked answer — matching each generated claim back to a specific retrieved result and refusing (not softening) any claim that doesn't trace to one, rather than trusting the model's own citation formatting.
**Why it's worth it:** "cites live sources or refuses" is a genuinely different, more defensible product than a static-RAG chatbot, it reuses a tool and a control-flow discipline both already proven in production, and it hardens a live-retrieval grounding pattern this profile hasn't built before — the only thing standing between it and the already-shipped kit is the retrieval source, which is the actual new work.
**First step today:** take finance_bot's existing Tavily call and wrap it in a single function that returns `(claim, source_url)` pairs instead of raw search text, then write one test asserting the function refuses to emit a claim with no matching source pair.

### 4. Agent-Spectated Poker Table — [Stretch]
- competence: unlabeled
- labeled_at: —

**Idea:** Expose poker-llm's live game state (hand history, pot, action-to-act, blind level) through a WebMCP tool surface modeled directly on CoCircuit's — read-only tools first (query current hand, query leaderboard, query a player's action history), then a "propose an action" tool for a spectating agent to suggest a move to a human-controlled seat — pairing two already-shipped `[emerging]` capabilities (Game-engine implementation + multi-LLM benchmarking harness, and Agent-accessible application interfaces) that have never been combined, only named together once in passing.
**Leverages:** Agent-accessible application interfaces (WebMCP) [emerging] (CoCircuit's 9-tool pattern, its viewport-follow-for-agent-edits convention, and its actionable-error-message discipline) and Game-engine implementation + multi-LLM benchmarking harness [emerging] (poker-llm's complete rules engine and FastAPI backend).
**One new thing to learn:** designing a WebMCP tool surface for a turn-based, hidden-information game (hole cards, action legality) rather than CoCircuit's fully-visible circuit graph — deciding what a connected agent may query versus what would leak information a human opponent wouldn't have.
**Why it's worth it:** this is a genuinely new pairing, not a repackaging of either shipped piece alone — it's the first reach toward "building software for an agent to operate" in a domain with hidden state and competitive stakes, a harder and more novel problem than CoCircuit's fully-observable canvas, and it directly serves this profile's own stated interest in benchmarking and operating LLM agents live rather than only in batch.
**First step today:** define and expose the single read-only tool `get_current_hand_state()` (pot, board, action history, whose turn it is — no hole cards) against poker-llm's FastAPI backend, and confirm a browser-console WebMCP client can call it mid-hand.

<!-- prompt-version: 2026-08-14.1 -->
